# @generated by Hollywood. Do not edit by hand.

name: CLI SDK CI
on:
  push: {}
  pull_request: {}
permissions:
  contents: read
jobs:
  verify:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: "24"
      - uses: pnpm/action-setup@a15d269cd4658e1107c09f1fabf4cbd7bd1f308a
        with:
          version: 10.34.5
      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts
        shell: bash
      - name: Generate workflows
        run: pnpm run ci:generate
        shell: bash
      - name: Check generated workflows
        run: git diff --exit-code -- .github/workflows/sdk-ci.yml
        shell: bash
      - name: Build CLI
        run: pnpm run build
        shell: bash
      - name: Check public source and build
        run: pnpm run public:check
        shell: bash
      - name: Test public scanner
        run: pnpm run test:public
        shell: bash
      - name: Install and test preview package
        run: pnpm run test:package
        shell: bash
      - name: Upload tested preview package
        uses: actions/upload-artifact@v7
        with:
          name: dedalus-cli-preview
          path: artifacts/*
          if-no-files-found: error
          retention-days: 7
